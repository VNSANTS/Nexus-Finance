node_modules/
dist/
dist-ssr/
*.local

# Variáveis de ambiente — nunca commitar chaves reais.
# .env.example fica versionado (sem valores reais); .env.local não.
.env
.env.local
.env.*.local

# Editor / SO
.DS_Store
.vscode/*
!.vscode/extensions.json

# Android/Capacitor — a pasta android/ É versionada (projeto nativo
# gerado por `npx cap add android`), mas os artefatos de build dela não.
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/*.iml
android/app/release/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
